"""Repeated words,
split the senttence into words
entDict={}
Loop:convert to uniform case
      for name sentlist:
        for key value in sentDict:
          if name==key:
           value+=1
          else:
             entDict[name]=1
"""
